#include colors.yml
#include widgets.yml

Form: !Form
    title: "Motors"
    margins: 5x5x5x5
    background: $757687
    
    
motor_grid: !grid

    repeat-over: "MOTORS"
    start-at: 1
    
    aspect-ratio: "{ASPECT}"
    
    padding: "{PADDING}"
    
    children:
        - !Apply:EmbeddedMotor
            background: *grey_lighter
            motor-pv: "$(P)$(M{N})"
