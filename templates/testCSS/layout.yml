#include colors.yml
#include widgets.yml

Form: !Form
    geometry: 1000x1000


Test: !vrepeat
    repeat_over: "INDICES"
    
    children:
        - !Label
            geometry: 120x20
            
            text: "Label {N}"
            transparent: false
            background_color: *header_blue
            foreground_color: Attention

Test2: !TextUpdate
    geometry: 500x500 x 100x100
    
    background_color: *edit_blue
    
    horizontal_alignment: "right"
